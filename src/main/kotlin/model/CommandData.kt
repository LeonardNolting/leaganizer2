package model

import com.jessecorbett.diskord.api.model.Message

/*
data class CommandData (
	val message: Message,
	val parts: List<String>,
	val parameters: CommandParameters,
	val reply: suspend (String) -> Unit,
	val fail: suspend (String) -> Unit
)*/
